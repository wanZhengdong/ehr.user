<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pd.ehr.dao.IUserDao">
	<typeAliases>
		<typeAlias type="com.pd.ehr.vo.User" alias="User" />
	</typeAliases>
	<select id="search" parameterType="User" resultType="User">
		select *
		from user where user.userName = #{name}
	</select>

	<insert id="add" parameterType="User" useGeneratedKeys="true"
		keyProperty="id">
		insert into user(userName,userAge,userAddress)
		values(#{userName},#{userAge},#{userAddress})
	</insert>

	<update id="update" parameterType="User">
		update user set
		userName=#{userName},userAge=#{userAge},userAddress=#{userAddress}
		where id=#{id}
	</update>

	<delete id="delete" parameterType="User">
		delete from user where
		id=#{id}
	</delete>

</mapper>